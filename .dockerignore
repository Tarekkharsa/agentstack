# Keep docker build contexts (repo root) lean: the egress-proxy image build
# needs the workspace SOURCE only.
target/
.git/
.github/
docs/
demo/
*.cast
*.gif
# Local agent-CLI state (root-anchored so example fixtures stay in): the
# session/config dirs agent CLIs generate in this checkout are not workspace
# source and can hold gigabytes of unrelated state.
.claude/
.cursor/
.codex/
.gemini/
.agentstack/
