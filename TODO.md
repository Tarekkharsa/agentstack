# AgentStack work queue

> **Purpose:** the only ordered product-wide work queue
>
> **Status:** re-seeded 2026-08-02 from `STRATEGY.md` v3 ("The plan"). The
> queue is the maintainer's to reorder; deviations edit this file, never the
> strategy. The strategy reopens only at its named revisit triggers.

1. [x] W2 — trust checked at dispatch (security first; hardens the shipped lease path)
2. [x] The delivery arc — W1, W3, W5, then W4 last; dynamic becomes the default at arc-end
3. [x] Library inversion — link-your-folders onboarding (multiple sources), library-first authoring, `init` imports into a linked folder
4. [x] Instructions — per-(CLI, model) variants over the injection channels, with the per-harness honesty matrix
5. [x] Surface finish — grouped review card; `run` locked by default; Varlock productization
6. [x] Workflows promotion — per-role model/effort, algorithm helpers, security findings closed, un-hidden
7. [x] Packaging — toolset into a self-run image
8. [x] The panel — the new surfaces over the existing ui-contract
9. [x] Vocabulary, settled 2026-08-02 — three renames before the bar is judged, so the surface being judged is the final one:
      - **`toolsets` becomes the manifest key.** `[toolsets.X]` is real; `[profiles.X]` keeps working as a silent alias so no existing manifest breaks. `--toolset` is primary, `--profile` an accepted older spelling (`run` already does this). Toolset is one of the four ideas; the mechanism noun must not outrank it in the file users read.
      - **"Package" means the library composition only.** The image artifact is only ever called an image (`agentstack image` already). A copy audit removes "package" wherever it means the artifact.
      - **The Mode axis retires.** `status` shows Delivery alone — what happened, per harness, and where. `set-mode` is retired and its ui-contract feature string marked superseded; the contract is versioned for exactly this. Keeping Mode would reintroduce the concept v3 deleted.
10. [x] Surface reduction — adopted 2026-08-04, before the bar is judged, so the surface being judged is the final one. `agentstack --help` lists fifteen verbs, not eighteen: init, status, add, search, apply, doctor, lock, toolset, use, yes, run, trust, undo, adopt, secret. Everything else moves one hop behind `agentstack x <cmd>`, grouped by task.
      - **The rule, not taste.** A command stays visible when a first-run surface, a doctor fix line, or a machine-readable field can name it. That rule promoted `lock`, `secret` and `adopt` and demoted `up`, `share`, `receive`, `workflow` and `restore`. Fifteen is the honest count the rule produces; ten was the wish.
      - **Nothing is removed.** Every hidden command still runs at its own name with its own `--help`; `agentstack x <cmd> …` is the same parse tree, dispatch and exit code. `agentstack --help --all` still shows the whole map, and the panel's fixed action names are untouched.
      - **A surface never names a command a reader cannot find.** Guidance that points at a hidden command now names it through the namespace (`agentstack x guard install`), and plain `--help` still lists, by name, every hidden verb guidance can print.
11. [ ] Review closures 2026-08-05 — what the blueprint review left in the code, ordered by whether it can hurt someone. `plan/work-plan.html` is the corrected board; this queue is the authority where the two disagree.
      - **[x] STOP-SHIP — hooks rendered with no trust gate.** `apply --write` compiled `[hooks.*]` into `.claude/settings.json` and `~/.claude/settings.json` for untrusted and stale-trust projects, exit 0, no warning — the one kind that never gets a compressed consent path. Gated like extensions, witnessed for both states at both scopes with negative controls, and `ENFORCEMENT.md:222`/`:647` now describe what the code does.
      - **[x] The small fixes.** doctor's Hooks check judged the global file for a repo project · F5 the ARCHITECTURE.md crate-edge rule is now parsed and checked, not remembered · stale `delivery --json` route text · the extensions gate refused a machine manifest that no `trust` command can reach. **N5 did not reproduce** — twelve runs across three `--manifest-dir` spellings and four working directories gave byte-identical JSON, so the two observed runs saw different state or an older binary; `doctor_cwd_independence.rs` guards it anyway. The two real cwd seams it did surface are fixed: `trust` never read `--manifest-dir`, and the process cwd outranked `AGENTSTACK_MANIFEST_DIR`.
      - **[x] N7 → P9.** The study kit is self-consistent again, so **nothing now blocks P9 but running it**.
      - **[x] N2, read then applied.** The audit is `plan/enforcement-claim-audit-2026-08-05.md`; twenty claims are narrowed to what the code does. **[x] P8** stayed open because `run` and `workflow` were never exercised and six journey screens were sketches; both halves are now done. All five `run` postures were driven, including a real Docker sandbox and a real lockdown run where the kernel refused the workspace write, plus a full `workflow` lifecycle; the six screens are machine-captured stdout in `plan/p8-journey-screens.md`, each labelled with its binary, its cwd and its own exit line. **P8 closed as an EVIDENCE pass** — its purpose was to find out what those surfaces really do, and it did. The captures were taken from a clean `git archive HEAD` build, so no in-flight working-tree edit could reach the binary; that is why they are trustworthy. **The eight P8-G gaps it produced are queued separately and are NOT part of this closure** — P8 being done does not mean they are.
      - **[ ] The gaps narrowing leaves standing.** Each is a real absence the document no longer hides. Ordered by what a hostile repository could use.
        - **[x] G1** `use --write` materialized skill files with no trust check. `render::skills::plan` now records the refusal and `render::skills::materialize` enforces it, so the additive `add skill --write` path is gated by the same choke point; the lock gate is untouched (`Unpinned` still passes — the first pin is the consenting act) and removal-only plans still run untrusted. Witnessed for both states at both scopes with negative controls, plus the prune and machine-manifest exemptions, in `red_team_skills_trust_gate.rs`.
        - **[x] G2** `apply --write` wrote native server config for an untrusted project. `render::apply::trust_refusal` records it on the plan and `TargetPlan::write` re-enforces it, so `apply`, `use` and `doctor --fix` all refuse alike; pruning what we already own and the machine manifest stay outside it. Witnessed in `red_team_servers_trust_gate.rs`. **The self-refusal the gate caused is resolved, and neither way the choice was framed:** `add <skill> --write` and the panel's edit verbs rewrite the manifest — the consent digest — and then materialize/re-render in the same run, so their own edit re-gated them. They now carry a `render::PriorTrust` captured before that write, and the gate judges the delivery against it: a command cannot be allowed to refuse itself, and the bytes that moved the state are the ones the human typed the command to add, so nothing new is authorized. **Trust is NOT re-pinned** (the difference from the owned-server refresh, whose bytes are machine-derived): the project is left `Changed`, the next command re-gates it, and the human still meets the review. `PriorTrust::STRICT` is the `Default`, so every non-authoring caller keeps the unrelaxed gate. `add_skill.rs` and `t3code_parity.rs::panel_remove_capability_updates_manifest_and_rendered_config` are green, and three witnesses in `red_team_skills_trust_gate.rs` prove the relaxation is not a hole — untrusted-before and drifted-before adds deliver nothing, and a reviewed add delivers once and then owes the next review.
        - **G3** an `owner`-tagged server's command line changes with no human re-review, because apply auto-repins trust. Show the refreshed definition in a card, or drop the auto-repin.
        - **G4** only `Gateway::from_frozen` carries the hard trust gate; extend `require_trust` to the host and lease constructors.
        - **[~] G6** file-tool write confinement covers only the fixed `WRITERS` names, and Cursor file writes never reach the guard — build a payload-shaped classifier; wire Cursor when it ships a pre-write hook. **The classifier half shipped in `905b11d`:** `classify_tool` now judges the payload SHAPE (`payload_is_write`) as well as the name, so `WRITERS` is a FLOOR rather than the whole rule and a tool this build has never heard of still gets write-checked; the editor-verb dialect is judged by its verb instead of short-circuiting to the shell branch, which is what made `str_replace_editor` dead on the list it was on. **Cursor is still not wired, and cannot be from here:** the repo's descriptor records its MCP config only, and the two hook events the guard can read are `beforeShellExecution` and `beforeReadFile` — there is no pre-write hook for a file write to arrive on. Same shape as G19: the remaining half waits on a surface someone else has to ship.
        - **[x] G7** instruction compilation never called `trust::check`, so an unreviewed repository's prose went straight into the managed region of `CLAUDE.md` / `AGENTS.md` — the one delivery that reaches a model's context with nothing to intercept it. `render::instructions::trust_refusal` now records the refusal on the plan and `InstrPlan::write` re-enforces it, so `apply --write` and `x instructions --write` both exit nonzero through the existing blocked-target seam, naming `agentstack trust .`; a refused plan writes nothing, so an approved region is left alone rather than emptied. The lock gate is untouched (unpinned still passes — the first pin is the consenting act), and the two gates are now visibly orthogonal in `content_pinning.rs`. **Package instruction members are gated as the project's content**: their pins ride the project's lockfile and they compile into the same region, so moving house rules behind a package buys no exemption. Outside the gate, each because it is not the project's content: emptying a region, machine-layer (`from_user_layer`) fragments — which must keep compiling for an untrusted repo, since they are the user's own house rules — the machine manifest at `$AGENTSTACK_HOME`, and the pre-command state a self-authoring command is judged against (`lock --write` and `upgrade` pin and then render in one run, and a command cannot refuse itself). Witnessed for both states at both scopes with negative controls, plus all three exemptions, in `red_team_instructions_trust_gate.rs`.
        - **G8** `--allow-unresolved` writes a literal `${NAME}` into live config — omit the key instead, or remove the flag.
        - **[x] G9** the write-time egress refusal in `render::apply` records nothing. Closed in `905b11d`: `record_egress_refusal` files both arms — a host the effective `[policy.egress]` denies, and a declared URL host that cannot be verified — through the same two seatbelt recorders the gateway's build-time egress check already used, so one incident reads the same wherever it happened, naming the declared host and never the URL. It records and never gates: the refusal is committed before the recorder runs, so no failure inside the evidence path can let a fail-closed render through.
        - **G10** the execution relay binds `0.0.0.0` when the gateway is undetermined and on unrecognized platforms — fail closed, or require an opt-in.
        - **[x] G11** the snapshot store and standing decisions are delivery inputs, not display state; a missing snapshot should re-gate, not drop. Closed in `905b11d`, witnessed by `crates/cli/tests/keep_pinned_fails_closed.rs`, which is written to this item's own framing: it drives all five paths that consume the snapshot and the standing decision — `use` materialization, the MCP catalog, `agentstack_load`, instruction compilation, the protected `--locked` run — in three snapshot states, with the healthy case as the control that proves the assertions are about the failure path. **Narrower than "re-gate" on two of the five, and say so:** `use` and instruction compilation EXCLUDE the item and name it with the reason and `agentstack trust` rather than refusing the run, and the catalog keeps the name but demotes it to `loadable: false` because an inventory that hides a name is the same silent omission; the outright refusals are `agentstack_load` and the protected run. Path 5 never reads the snapshot at all — a keep-pinned item leaves the lock on the APPROVED bytes while the project copy holds the declined ones, so strict verification already refuses it; what changed there is the remedy, which used to name `lock --write`, a command that by design will not move a decided pin.
        - **[x] G13** the three fail-closed guard system refusals write no audit line. Closed in `8ae1569`: all three (`machine-config-unreadable`, `machine-policy-unavailable`, `hook-payload-unreadable`) go through one `refuse` helper, so the wording, the subject and the fail-closed exit cannot drift apart between them, and each appends under a synthetic `system: ` subject that a call's own payload can never forge — which is what keeps a guard failure tellable apart from a rule denial in the same log. Witnessed in `guard_system_refusal_audit.rs`.
        - **[x] G14** the executor result-file bind has no kernel write cap; 1 MiB is a host read refusal. Closed in `8ae1569`: the hardened executor profile sets `RLIMIT_FSIZE` (`SandboxSecurity::file_size_bytes`, 4 MiB) with soft == hard, so the guest cannot raise its own limit. That is the only kernel bound that REACHES a read-write file bind — a tmpfs size caps a container-private filesystem, while a bind's bytes land in the host inode, so the limit has to sit on the writer. Four times the host's 1 MiB read refusal deliberately, so the cap can never clip a result the host would have accepted; witnessed in `sandbox_result_cap.rs`.
        - **G15** `max_wall_seconds` is inert in the engine.
        - **[x] G16** `set_decision` mutates the trust store and appends nothing. Closed in `8ae1569`: it now appends `decide`/`undecide` through `record_mutation` after the save, identity only — the item, the answer and the pin a keep-pinned answer names are consent CONTENT and stay out of the log, so the line carries the direction and nothing else. A call that changes no bytes neither saves nor logs, which keeps the crate's "one store write, one line" invariant and stops empty `undecide` lines drowning the real answers.
        - **[x] G17** `Family::Fence` has no `RunEvent`, so fence refusals never reach a run report. Closed in `8ae1569`: `RunEvent::FenceRefused` exists, `seatbelt::record_fence_refused` appends it, and `report` renders a **Fence refusals** section of its own. Its own section on purpose — a refused call is not a call the run made, so folding it into Tool calls would swell the count a reviewer reads as this run's gateway actions. The witness was caught passing while the report printed nothing, and was mutation-checked after the fix.
        - **[x] G18** settings are not pinned at all — `LockedSetting`, a re-gate on change, a doctor probe, a witness. On no queue item until now. Closed in `e69fdf5`: `LockedSetting` pins per (target, key) — the grain agentstack actually owns, so an undeclared key in the harness's own file can never read as drift — over the value AS DECLARED with `${REF}`s unresolved, because a resolved value is machine-specific and can be a secret. `doctor`'s probe never collapses the two legs, which have different fixes: declaration↔pin is `lock --write`, declaration↔disk is `apply --write`, and a lock predating settings pins degrades to leg 2 alone rather than erroring. **The re-gate half landed as a WARNING, not a refusal, and that was the call:** settings are inert config merged into a file the harness owns, so failing closed would leave the user's own config half-written — the review the item wanted already fires through the manifest bytes, since editing a declared value moves the consent digest. Witnessed in `settings_pinning.rs`.
        - **[~] G19** Kiro gets no host guard. The claim that "its hooks nest inside per-agent config files" was never supported by anything in this repo — the descriptor records Kiro's MCP config only, so there is no hook path and no entry shape uninstall could find again. The code now says which kind of gap it is (`NOT_WIRED`, a fact about agentstack) instead of implying none could exist; wiring it still needs a descriptor that records the hook surface, and that needs a source.
        - **[x] G21** extension, workflow and blueprint checksums never deposit into the content store, so a re-gate can show a pin with no approved copy to diff. Closed in `311e088`: extensions and workflows deposit through `Store::pin_integrity_root` and a workflow's approved blueprint through `pin_blueprint`, so every path-sourced pin in the lock now has approved bytes a re-gate can diff against. The integrity-root digest family had to be taught to the store's READER first — without that a verbatim deposit would still have read as unverified, which is a worse answer than none. Bounded at 500 files / 8 MiB to match the diff renderer's own caps, and best-effort throughout: a failed deposit never fails the pin, and no lockfile field or pin digest moved. Witnessed by `every_path_sourced_pin_in_the_lock_has_its_bytes_in_the_content_store` in `pin_deposits_bytes.rs`.
        - **G5** there is no headless path for unsigned content, since `receive --yes` refuses it — add a `--consented-digest` acceptance bound to previewed bytes, if that path is wanted at all.
      - **Decided 2026-08-05.** Two consent-model calls the gates forced, both adopted: a command may not refuse itself (judge the gate against the trust state at command start, captured pre-write, and do **not** re-pin — the project stays `Changed` and the next command re-gates it); and `lock --write` on drifted content no longer delivers it, because re-locking accepts the CONTENT and does not answer the consent question it just reopened. **Verb count:** honour item 10 — `lib` and `why` move behind `agentstack x`, fifteen stands, and the rule decides which verbs are visible, not the number.
      - **Still yours, and deliberately not scheduled:** R2 the positioning flip (the strategy reopens only at its named triggers) · R1 the private APM disclosure — **maintainer-only: embargoed to a third party's private channel, so no agent drafts, sends, or contacts anyone about it** · the Mode-axis leftovers still in `doctor --json`, `overview.rs` and `ui_contract.rs` · P2 `automatic-delivery.md` · O2 · O3 · F6 · F8 · `x diff --profile`.
      - **The remaining gap decisions** (each reverses or narrows an adopted design, so none is a cleanup): **G3** an `owner`-tagged server's command line changes with no re-review · **G4** extend `require_trust` to the host and lease gateways, which revokes "naming the manifest dir is the consent" · **G5** there is no headless path for unsigned content · **G8** `--allow-unresolved` writes a literal `${NAME}` into live config · **G10** the relay binds `0.0.0.0` when the gateway is undetermined.
      - **[x] G22 — three next-step lines name a command that then refuses.** The gate reached five kinds; three footers did not follow, so each sends the user to a command that fails. Reproduced against 0.18.0-rc.2 (the first two confirmed twice): `lock --write`, on a project its own write just made stale, warns "re-review and re-grant with `agentstack trust .`" and then prints `Next: agentstack use --write` · `toolset create` re-pins the lock and prints `Switch to it: agentstack use <name> --write` · `add skill --write` in clean-at-rest prints `next session picks this up: agentstack x session start <toolset>`, because its footer branches on delivery mode and never on trust state (`commands/add.rs:1563`). Same class the `doctor` hooks fix closed: name the command that unblocks the state the user is now in, decided from the real trust state after the command's own write (`trust::check`; `render::PriorTrust` and `trust_carry` already ask that question — do not add a fourth way). The how-to pages document the trap in the meantime, so this is honest, not hidden. Closed: each footer now reads the POST-write trust state (`trust::check`, shared as `lock::review_pending`) — deliberately not `PriorTrust`/`TrustCarry`, which relax a gate against a PRE-write capture and would tell a user everything is fine exactly when a review is due. `toolset create` turned out to be a fixed-argv alias of the panel action, so the fix landed in `panel_edit.rs`.
      - **[x] G23 — a refusal can print above a zero exit.** When a gated project's delivered bytes ALREADY match what is declared, the plan is unchanged, so nothing is blocked and `apply --write` exits 0 — but the `✗ refusing to render …` line still prints and still raises the issue count. A printed error with a success status is a script hazard, and a refusal that claims to withhold bytes which were never going to move is simply untrue. The **server** path already has the guard (report the refusal only when the plan would deliver something; "refusing to render" above "✓ up to date" is two contradictory claims) — **hooks and instructions do not**, and skills is unchecked. Note for whoever takes it: the gate itself must not move, only whether the refusal is *reported*; `HooksPlan::write` refusing on `refusal` alone is correct and stays. Closed on the second attempt, with the guard the first one only described: `refusal.is_some() && changed()`, copied from the servers spelling, in `apply`'s hooks and instructions arms and in `x instructions --write`. **Skills needed nothing and cannot regress** — `trust_refusal` returns `None` for an empty active set, so `refusal.is_some()` structurally implies `has_work()`. Reproduced by appending a COMMENT to the manifest: the consent digest moves, so trust goes stale, while no rendered byte does.
      - **[x] G24 — a dry run promises what the write would refuse.** `add`'s static-unambiguous preview prints `→ will materialize into N targets` without branching on trust, so on an untrusted static project the preview promises a materialization the later `--write` refuses. Same family as G22, found while closing it, and closed on the same `review_pending` reading. The lane audit found it was the LAST of its family, not one of four: the static-ambiguous and clean-at-rest previews were branched by G22 and zero-files never promised delivery. The preview still states the delivery size — going quiet would be worse — and names what blocks it; a trusted project's line is byte-for-byte unchanged. The static-unambiguous WRITE path is untouched, and cannot be reached by this arm. **Correction, 2026-08-06: "the LAST of its family" was wrong.** The lane audit that produced that claim covered `add`'s preview lanes only; the P8 evidence pass then found two more of exactly that family in other commands — **P8-G2** (`use`'s dry run closes "Re-run with --write to apply." on an untrusted project, and `use --write` exits 1) and **P8-G3** (the same shape in `apply`). The G24 fix itself still holds and is not reopened; only its scope claim was overstated, and the family is now known to reach past `add`.
      - **[~] G25 — the guard reads a shell command as a string only, so an argv ARRAY bypasses it.** `crates/cli/src/guard.rs:1051` reads `command` with `as_str()`; there is no array handling anywhere in the file. A harness that emits argv as a list therefore skips the whole destructive-command check — `rm -rf` outside the workspace, `git reset --hard`, the lot. **Codex is the suspected caller** and no fixture pins its real payload shape; only Claude, Cursor and Antigravity ever reach `parse_event` in the inline tests. Two smaller holes ride along: Codex's `apply_patch` sits in `WRITERS` but is inert, because its args carry patch text and no path key, so `path_from_input` returns nothing and the call short-circuits to `GuardEvent::Other` — allowed outright, not even deny-globbed; and Antigravity's `replace_file_content` matches neither the name list nor `payload_is_write` (its `ReplacementChunks` is an array, while the body-key rule requires a string). **The argv hole is closed, shape-agnostically and without guessing a vendor schema.** `command_line_at` reads a command as a string OR an argv array, joins the words, and hands them to the same `check_bash` the string path uses — one command path to review. Each word is QUOTED before the join, because an argv word is literal data: unquoted, a `;` or `>` inside an argument becomes shell structure nobody asked for, and a commit message could refuse its own commit. Applied at all four payload sites. Nested arrays flatten to depth 4, non-string elements drop, an unreadable array behaves exactly as before — a mixed array can only ADD analysis. `payload_is_write` also accepts the `replacement*` key family, closing `ReplacementChunks` without knowing what a chunk contains. Each fix was reverted separately to prove its witness; the anti-wedge control passes in every state. **Residual:** `apply_patch` stays inert — its args carry patch text and no path key, and `GuardEvent` has no write without a path, so a perfect classifier cannot help; finding the path means parsing Codex's `*** Begin Patch` envelope, which no fixture pins, and that is the guess that produced the dead `str_replace_editor` entry. No Gemini fixture and no Codex SHELL fixture exist. An argv field named `args`/`argv` rather than `command` is still missed, deliberately. The dimension stays cooperative. **Also found:** the example scripts call `mktemp -d`, and macOS ignores `TMPDIR` for that form, so their sandbox always sits under `/var/folders`, an always-writable temp root — their assertions do not depend on it, but no script can test workspace confinement through its own sandbox path. **Re-read 2026-08-06:** the argv work above landed; the residual is unchanged, so the marker stays `[~]` and the item stays open on that residual alone.
      - **[x] G26 — moot: the page was deleted.** `docs/panel/index.html` carries a badge reading "32 of 32 · captured · ran against the real binary" (and a matching claim at ~line 314) while several samples predate this release. Nothing validates the page — the same structural hole as the tutorial. The Kiro guard lines are fixed; a survey named the rest, verified against the binary: `CLI.help` still lists `up`/`share`/`receive`/`restore` and omits `lock` and `secret` (the real top level is the fifteen, plus the `agentstack x` trailer) · `CLI.helpall` misses `unrender`, `delivery`, `why`, `image`, `lease` · the contract block predates `set-mode`'s retirement and three reworded actions · `CLI.status` and `CLI.trustAfter` still print a `Mode` line the product removed, in place of the Delivery and Context blocks · `CLI.doctor` predates the t3code section, the two settings-drift legs, and doctor naming `agentstack trust .` · `CLI.degraded` is labelled `doctor` but shows output no doctor invocation produces. Genuine GAPS, not staleness: no `delivery` sample at all, no trust refusals for the five kinds, no fence refusals in a run report. **Done:** the six named samples are re-captured verbatim against 0.18.0-rc.2, `CLI.degraded` is relabelled to its real source (`status --json | jq -r '.features[]'`, not `doctor`), and the blanket badge is replaced by three states whose counts derive from the data — captured 6 · reconstructed 9 · **not re-checked 17**, the last reading "believable, not verified — check it against the CLI before you rely on it". That is the point of the item: a provenance claim is worse than a stale sample, so the page now says which is which. **Left:** re-run the 17. The remaining 17 will not be re-run: `docs/panel/index.html` is gone, replaced by a redirect stub to `integrations.html#the-panel-journey`. It was 2,293 lines of JavaScript simulating a second UI, which CLAUDE.md forbids outright. The salvage pass found exactly one thing that lived only there — the `edit-profile --preview` payload shape, now in `automation.md` — and one thing that lived nowhere else either: **`apply-v1`, a contract name the page invented**, present in no source file.
      - **[x] G28 — three more rungs emit no machine command, for the same placeholder reason G27 hit.** `machine_command` drops any string containing angle brackets, deliberately — a panel must not offer a command a user cannot run verbatim — but the rungs behind them were written as templates, so `next_action.command` is `null` while the sentence reads fine. Largest first: **`Rung::Group`** (`agentstack toolset create <name> --server <server>`), emitted by BOTH `status` and `doctor` (`doctor.rs:569`), so a trusted, rendered, ungrouped project gets nothing · **`EMPTY_PROJECT_NEXT`** (`agentstack search <query>`), both surfaces · **`clean_at_rest_next_step`** (`agentstack x session start <toolset>`, `overview.rs:1908`) when the manifest holds 0 or 2+ toolsets. Each needs a concrete command or an honest admission that the next step needs an argument only the human can choose — do NOT weaken the bracket rule, which is what makes the gap visible. Related residual: an EMPTY untrusted project gets `agentstack trust .` from `doctor` and `agentstack search <query>` from `status`, so the two surfaces still disagree in that one state. **Closed, verified with a per-rung table measured from the binary rather than read from the source:** `Rung::Group` on BOTH surfaces, and `clean_at_rest_next_step` at 0 toolsets and at 2+ toolsets, all emit a documented null; the 1-toolset control still emits `agentstack x session start dev`, which is what proves the null is not blanket. **`EMPTY_PROJECT_NEXT` is closed too**, and it moved after that table was taken, which is why the table looked short of the three rungs. Its machine field is now `null` — the special arm in `machine_command` was deleted, so it falls through exactly like `Group` and `Verified` — and its sentence teaches shapes instead: a bare `agentstack search`, plus `agentstack add server <name>` / `add skill <source>` / `add from <id>`. Every `add` form was measured at exit 2, because the argument is the one thing only the human can supply. **Why the field had to become null is the substance of the gap:** `guidance_is_executable` proved that naming `agentstack search` there handed a driver a read-only browse — run it, every observable field is identical, the same command is named again, and the loop reproduced. **The earlier claim that nulling it would be a schema break was WRONG, and is corrected here:** `Rung::Group` and `Rung::Verified` already emit null, and `converge_once` documents null as the terminal answer a driver stops on. The tail is closed too — both surfaces now agree on an empty untrusted project. The angle-bracket rule was NOT weakened.
      - **[x] G32 — two more of `up`'s steps report success over a failure, and one is sharper than G30.** **Lock verification** (`commands/up.rs` ~L101-113): when `install --locked` fails, `up` prints a yellow "could not verify against lock" and still exits 0 — while the comment four lines above states that `--locked` "is the one step of `up` that must not be best-effort". A CI job therefore reads success over an environment never verified against the lock. **Registry load** (`detected_harnesses`): `let Ok(registry) = … else { return Vec::new() }` makes an internal failure indistinguishable from a machine with no CLIs installed, so the user is told "found harnesses: none — install a supported CLI" and blamed for our error. Found while closing G30; same family as it and as the G22/G27 guidance work. Separately, and in `doctor.rs` rather than `up.rs`: on the nothing-delivered condition `doctor`'s next step is `agentstack apply --write` — the command that just failed identically — when the two commands the error itself names (`x gateway connect`, `x delivery render-locally`) are the real way forward. **Closed, all three, and verified to converge.** (a) Lock verification exits **1** now instead of printing "could not verify against lock" over exit 0 — `up` still renders and still prints the closing step, so only the verdict moved, which is what the comment four lines above already demanded. (b) `detected_harnesses` returns a `Result`, so OUR adapter-registry failure and a bare machine with no CLIs no longer print the byte-identical "none — install a supported CLI"; the two read differently. (c) `doctor`'s nothing-delivered next step names `agentstack x delivery render-locally --write` rather than `apply --write`, the command that had just exited 1. That one was chosen over the error's other named command deliberately: `x gateway connect --all --write` was MEASURED exiting 1 with no MCP-capable harness, so naming it would have repeated the bug being fixed.
      - **[x] G29 — `status` sends the user to `trust` before `lock`, and following it drifts them.** After `add server --write`, `status` prints `not locked (never activated)` and `Next: agentstack trust .` on consecutive lines. Obey it and the next `use --write` re-pins the lock, invalidating the grant made seconds earlier. The binary's own `agentstack yes` error text states the correct order (adopt → lock → trust → use), so the ladder disagrees with the product. **What makes it bite: `add server --write` does NOT lock, while `add skill --write` DOES** — same verb family, opposite behaviour. Reproduced: the lockfile gains its `[[server]]` entry during `use --write`. Fix the ordering rung and the add-family inconsistency together; either alone leaves the trap half-set. Same family as G22/G27 — guidance that names a step which then costs the user a review. **Closed, both halves, because either alone leaves the trap half-set.** The ordering half: `trust` pins the content digest of the manifest layers AND the lockfile, so a grant made before the pins exist is void the moment `use --write` writes them. Measured both ways — `add server --write` → `trust .` → `use --write` leaves `locked · trust stale (content changed)`, while the same run with `lock --write` BEFORE `trust .` leaves `locked · trusted`. The grant survives only when the pins came first. **It was the ladder that was wrong, not the product:** `agentstack yes` already printed `adopt --write` → `lock --write` → `trust .`, and `lock --write`'s own footer already ended with "Next: `agentstack trust .`". Only the ladder disagreed. **Locked projects still say `trust .`** — both branches are witnessed, and the clean-at-rest row (saved lockfile, untrusted) still asserts `trust .`. **The rung is kept honest by `lock_pins_something`:** a hooks-only manifest gets `lock --write`, exit 0, "pinned nothing new", and NO lockfile on disk (measured), so routing that project to `lock --write` would name a command that does nothing — which is the very bug family being fixed. The predicate prevents that. **The add-family half is also closed** — `add server --write` and `add skill --write` now agree on pinning, witnessed by `add_server_write_pins_in_the_same_command_that_declares` in `crates/cli/tests/add_skill.rs` (preview pins nothing; `--write` lands the manifest entry and a lockfile naming the server in the same command).
      - **[x] G30 — `up` exits 0 where `apply --write` exits 1, on the identical condition.** Everything routed live and no bridge registered: `apply --write` errors "nothing was delivered" and exits 1; `up` prints "rendering stopped early" and exits **0**. `up` is the documented new-machine command, so a setup that delivered nothing passes silently — and a script or CI job reads success. Closed: `up` adopts apply's exit for the WHOLE render step, not just this condition — its render IS `apply --write` (`write_quiet`), and two spellings of one operation may not report different verdicts on one project. The narrow fix was rejected deliberately: classifying apply's opaque error would mean matching its error text, i.e. a second drifting copy of apply's exit policy inside the one command whose design rule is that it owns no policy of its own. `up` still CONTINUES after a failed render (so the closing next-step still prints) — continuing and succeeding are separable, and only the second was wrong. Blocked-write parity witnessed too.
      - **[x] G31 — the undo ledger does not cover skill materialization.** `use --write` reports "wrote skills to 3 locations", then `undo` and `x restore --list` show only the `init` entry. `restore --help` lists servers, settings, hooks and instructions — skills are absent, so this may be intended, but "Undo: take it back" is a wider promise than the ledger keeps. Either record materialized skills or narrow the promise; `docs/start.md` already documents the narrower truth. **Closed by NARROWING THE PROMISE, not by widening the ledger, and the reason is the point:** the ledger stores file bytes, while a delivered skill is a symlink or a directory, so `capture` returns `None` for both; `rollback` then deletes by path with NO ownership test, so a skills-aware ledger would delete a hand-made skill directory. `unrender.rs` already marks its skills leg `capture: false` for exactly this reason. `undo`, `x restore`, both `--json` outputs and both `--help` texts now state the exclusion and name the way back — `agentstack x uninstall --write`, verified working. The notice is conditional: a project with no materialized skills prints nothing. **Residual closed in `b836611`, and it was wider than the two surfaces this item first named:** `x uninstall` promised the same undo, and five more surfaces said it too. **Six surfaces, bounded two ways, and the split is worth stating because a reader grepping for the constant will not find the sixth and must not conclude it was missed.** Five are bounded through G31's own shared constant, `history::SKILLS_ARE_NOT_RECORDED` — `uninstall.rs`, the `cli.rs` help text, `setup.rs`, `session.rs` and `mode_switch.rs` — so those wordings cannot drift apart again, which is the reason for a constant rather than five edits. The sixth is `unrender.rs`, whose MODULE HEADER now says it: every un-rendered FILE is undoable with `agentstack restore`, then a paragraph names the materialized-skills leg as the exception and points at `Removal::capture` and at `crate::history` for the reason. Prose, not the constant, because the constant is runtime output and this is a doc comment — so the header now agrees with the `capture: false` code beneath it. Evidence measured on disk: `x restore --last --write` returns `.mcp.json` and leaves `.claude/skills/greet` where it is, and a session that materializes ONLY skills records no ledger entry at all, so the command those surfaces named exits 1 with "nothing to undo". `session.rs` was the one that needed reading rather than replacing: only its parenthetical was false, and the first half of the sentence was true.
      - **[x] G27 — a fully blocked apply reports no next action.** A static, rendered, UNTRUSTED project emits `next_action: null` from `status --json` while `apply --write` is entirely refused: `next_step` falls through to the toolset rung, and `machine_command` then drops that string because it contains angle brackets. So the one surface a panel reads for "what should this person do" says nothing, in the exact state where the answer is `agentstack trust .`. Found while adding `trust_blocks_delivery` (which now at least makes the state legible); this is the routing half and is separate from the field. Same family as the G22 footers and the `doctor` hooks fix, and closed the same way. The rung reads `trust_blocks_delivery` — the reading added with the field, not a fourth test — and sits BELOW `adopt` and ABOVE the setup ladder: below `adopt` because `adopt` writes the manifest and a grant binds to the manifest, so reviewing first would cost the user a second review; above the setup ladder because the gate refuses every write those rungs ask for. It emits a concrete `agentstack trust .`, so the angle-bracket rule is untouched. It also closed a disagreement nobody had noticed: `doctor` already answered `agentstack trust .` for this exact project while `status` — the surface a panel reads — was the empty one.
      - **[x] P6 — the tutorial is a generated document again.** `docs/tutorial/index.html` carries a dated banner saying it is behind the binary, so it is honest today, but the lines themselves still teach the old product. Worst first: **183** ("nothing on rungs 1–5 requires any of them" — it tells the reader to ignore the gate, and rungs 2 and 4 both stop at it) · **441** (`use backend --write` shown succeeding, when line 440 re-locks and makes the grant stale) · **446**, **311**, **583**, **585**, **396**, **769**, **776**, **425** (sequences with no review step) · **281** (frames the gate as a fresh-clone concern; your own project is gated the same way after any edit). Independently of the gate: **377, 381-383, 580-589** teach the three-mode picker that TODO #9 retired (`set-mode-v1` is in `SUPERSEDED`), **415** sets `VERSION='0.17.1'` while 312 and 771 say v0.18.0+, **419-420**'s flagship demo output does not match what `init` and `apply --write` print, and **823** still spells `agentstack restore`. Correct and worth keeping: 588-589, 549, 551.  **Done:** every gate-related line above, plus the version constant (now 0.18.0-rc.2) and the `restore` spelling; the worst line — "nothing on rungs 1-5 requires any of them" — is gone. Verified by extracting the script and running `node --check`, because a stray quote blanks this page silently. The retired mode axis is gone too: `MODE` became `LANES`, carrying four real routings taken verbatim from the binary (a both-lanes tool, a live-only tool, a file-only tool, and the render-locally override), and the wizard step now REPORTS the routing rather than asking the reader to pick one. **The structural half is closed too, and cheaply.** `docs/tutorial.md` (343 lines) is now the source and the page is generated: 90,958 bytes of hand-written HTML became 31,589 generated, and **39,917 bytes of inline JavaScript became zero**. The conversion cost ONE line in `make-docs-pages.py` — the earlier "multi-day" estimate assumed keeping the four browser simulators, and deleting them removed the need for a new page kind. Eleven lessons became six steps; the cheat sheet, term cards, posture table and policy diagram went to `reference.md`, `concepts.md` and `enforcement.md`, which already held them. Kept because readers get them wrong: lock-before-trust, the five gated kinds with a real refusal transcript, and delivery routed rather than chosen. The honesty banner is gone because it is no longer true — the prose is checked like every other page now. One more stale line surfaced on the way: the page taught a plain `run` as HOST/ADVISORY, when the Protected tier is the default and `--unprotected` opts out. The structural fix is unchanged — give it a markdown source or drop it from the nav at `tools/make-docs-pages.py:539`; until one of those happens nothing stops the next seven false lines.
      - **[ ] The P8 evidence pass, 2026-08-06 — eight gaps, none of them fixed there.** `plan/p8-journey-screens.md` is the capture of record and holds the evidence for every item below: machine-captured stdout+stderr from two binaries built at `9aef01e` on `fix/followups-2026-08-06`, with two disclosed transformations (ANSI stripped, the lab path rewritten to `<lab>`). It supersedes `plan/p8-scope.md`'s appendix, which is headed "raw evidence" but abbreviates digests, elides lines, and once substitutes a description for the output. It is the pass that closed P8 above; these eight gaps are its output, not its unfinished business.
        - **[x] P8-G1** ANSI colour is unconditional. `crates/cli/src` contains ZERO occurrences of `NO_COLOR` or `no_color`, and `is_terminal` only gates whether to prompt, never whether to colour. Escapes therefore reach files, pipes and CI logs, and did so with `NO_COLOR=1` exported. Worth recording as an implication: every docs sample of these screens must have been hand-stripped by someone, which is a silent transformation no page discloses. **Closed in `b836611`.** One gate decides at startup — `CLICOLOR_FORCE`, then `NO_COLOR`, then `TERM=dumb`, then stdout-is-a-TTY — and it is asked at FORMAT time, so forced output is byte-identical to what shipped before. Measured: 16 escapes on a real pty, 0 under `NO_COLOR`, 0 piped, 0 under `TERM=dumb`, `--json` unaffected. **The part worth keeping is what now holds the rule:** `owo-colors` was REMOVED from `crates/cli/Cargo.toml`, so an ungated `use owo_colors::…` fails to COMPILE (`error[E0432]`) rather than failing a test — a compiler-enforced rule, not a remembered one. A first attempt bought "no call sites edited" with a new crate plus a dependency alias, and `check-structure` rejected it: an eleventh crate is an architecture change, and not one a colour fix gets to make quietly.
        - **[ ] P8-G2** `use`'s dry run promises what the write refuses. On an untrusted project `agentstack use <toolset>` exits 0 and closes "Re-run with --write to apply."; `use <toolset> --write` on that same state exits 1 and applies nothing. The `✗` blockers do print above the closing line, so it is not silent — the closing sentence is still a promise the write does not keep. G22/G24 family, and the instance G24's lane audit did not reach.
        - **[ ] P8-G3** `apply`'s dry run has the same shape. On a trusted, locked project routed entirely live with no bridge registered, `apply` exits 0 with "0 targets would change. Re-run with --write to write." and `apply --write` exits 1 with "nothing was delivered". The counted fact is honest; the instruction is not.
        - **[ ] P8-G4** `use --write` reports activation for a run that failed. The failing untrusted write prints `⚠ activated 'backend' on 4 targets (wrote 0); 3 targets BLOCKED: …` ABOVE `error: 3 targets blocked` and exit 1. Activation "on 4 targets" over zero files written, in a nonzero run, reads as a partial success where there was none; the four counted targets are the ones with nothing to do. G23 family.
        - **[x] P8-G5** `--unprotected` is unreachable non-interactively. `run <h> --unprotected --plan` exits 1 ("--plan needs a gated run mode"), and `run <h> --unprotected --prompt …` exits 1 ("--prompt needs the protected run"). So the documented escape hatch has no preview and no headless form. `docs/howto/lock-down-a-run.md` describes the flag and its banner with no mention of either restriction, and `plan/one-page-draft.md`'s posture table presents it beside three postures both flags accept. Decide whether the restriction is intended and document it, or widen the flags. **Closed in `b836611` as INTENDED, NOW DOCUMENTED — the item's first branch, not its second. Nothing was weakened, and that distinction is the entry:** widening either flag would have been a consent change, and this was a documentation absence. `git log -S` shows the commit that introduced the refusal replaced a fall-through that LAUNCHED the CLI during a read-only review. The rule is not about `--unprotected` at all — it fires for `--sandbox` and `--lockdown` too — so what it says is that headless delivery exists only inside the protected contract, which `locked.rs` hard-asserts for workflow children. `docs/reference.md` had already documented both restrictions with their reasons; only the howto and `plan/one-page-draft.md` omitted them, and both now state it. The refusals now say why.
        - **[ ] P8-G6** `workflow explain --json` does not carry the `role_details[]` the contract table promises, and this is **left unfixed ON PURPOSE**. `docs/automation.md` puts `x workflow list --json` and `x workflow explain --json` in one `workflow-role-selection-v1` row described as "per-entry `role_details[]`". `list` has it; `explain` emits a top-level `roles[]` with the same per-role fields and ZERO `role_details` keys anywhere, so a consumer implementing the documented contract against `explain` finds nothing. Choosing between "document the two different key paths" and "make `explain` emit `role_details`" is a CONTRACT decision for the maintainer, not a typo, which is why the evidence pass stopped here.
        - **[ ] P8-G7** a manifest schema error names neither the file nor the fix. A manifest missing the top-level `version` key fails with ``error: manifest does not match the expected schema: missing field `version` `` and exit 1 — no path, no line, no statement of what a valid header looks like; the same shape appears for a `[servers.X]` table missing `type`. Low severity, and it is the first error a hand-written manifest hits.
        - **[ ] P8-G8** an OBSERVED ASYMMETRY to confirm, not a defect claim: on an untrusted project `x workflow explain <name>` refuses (exit 1, "nothing from an untrusted bundle normalizes or is invocable") while `x workflow list` exits 0 and prints the workflow's name, roles and declared ceilings, correctly marked `TRUSTED false`. It looks deliberate — names and declared numbers are manifest metadata, not normalized script facts — and is queued so someone says so on the record.
        - **[ ] What P8 left unverified.** Each of these is believable, not verified — check it against the CLI before you rely on it. The `--unprotected` HOST/ADVISORY banner: not captured, because both non-interactive doors refuse it (P8-G5) and `script -q` could not allocate a pty, so nobody has seen it · real-model workflow semantics and the performance bookends in `examples/workflow-acceptance/README.md`: every workflow child was a ten-line shell script, which is the right harness for the governance claims and silent on model behaviour · macOS kernel containment: the read-only workspace refusal is real, but Docker on macOS is a Linux VM, so the kernel enforcing the bind is not this machine's · `agentstack x image --write`: only the plan screen was captured · and every other `docs/` sample not named on that page.
      - **[x] The guidance fence, 2026-08-08.** `crates/cli/tests/guidance_is_executable.rs` now sweeps the state matrix against every surface that prints a next step — 21 states, each naming the routing arm it exists to reach, over 13 read-only surfaces plus 5 writing surfaces on 6 states — and it prints its own coverage on a passing run, computed from the run's own record rather than written by hand. It also records IN FILE, and re-prints on every run, the nine things it does NOT reach and why. **The reasoning worth keeping is why the coverage report had to be derived and not authored.** An earlier version of that file printed "nothing — every surface answered in every state" while dropping machine-field prose on the floor and never reaching a healthy project at all. And a rung recorded there as structurally unreachable — `Rung::Group` on `status`, on the reasoning that `run` spawns with `env_clear` and a bare `PATH` so no harness is ever detected — turned out to be reachable: harness detection reads the harness's OWN config file, and `~/.claude.json` lands inside an isolated HOME. The gap was in the fixtures, not in the isolation. **"A blind spot recorded as structural is worse than one recorded as work outstanding: the first stops anybody looking."** The four gaps below are its output.
      - **[ ] The guidance sweep, 2026-08-08 — four gaps, none of them fixed there.** Each is an `#[ignore]`d reproducer in `guidance_is_executable.rs` with the defect named in its ignore message, listed in that file's `KNOWN_DEFECTS`. The ledger is not an allow-list: every entry must STILL REPRODUCE or the run fails and demands the entry's deletion, so a repaired defect cannot leave a suppression behind it. **G33 and G34 are the MACHINE-FACING half of the G22/G24/G27/G29/G30/G32/P8-G2/P8-G3/P8-G4 family, and that half had never been swept before** — a panel or CI driver reading `next_action` has no terminal, so it is handed a command that cannot succeed.
        - **[ ] G33** the machine field of an UNADOPTED directory names a command that cannot run without a terminal. State: no manifest at or above the working directory. Both `doctor --json` `/next_action` and `status --json` `/next_action/command` read `agentstack init`, which exits 1 with "refusing to init without a terminal". A machine field is executed verbatim by a driver, and a driver has no terminal, so it errors, re-polls, and reads the identical field forever. The runnable spellings exist — `init --dry-run`, `init --yes`, `init --secrets <store>` — and `init`'s own refusal prints all three, so the field names the one form that cannot work.
        - **[ ] G34** `status --json` hands a driver `agentstack yes`, which needs a terminal. States: `empty-manifest`, `servers-no-bridge`, `dropped-undeclared` — every project with a file dropped in that the manifest does not declare, which is the `undeclared_drops` arm of `overview::next_step` and outranks every other rung. `agentstack yes` exits 1: "`agentstack yes` needs a terminal — it is a review you read and answer." The refusal then names the headless path in full, so a runnable first step does exist (`agentstack adopt --write`) and the machine field does not name it. **`doctor --json` answers `null` for the same project**, so this is invisible to any check that reads only `doctor`.
        - **[ ] G35** the `lock --write` regression, alive again in a second verb. State: a trusted project, a detected harness, and a server configured natively in `.mcp.json` that the manifest does not declare — the `unimported_native` arm. Both `doctor --json` `/next_action` and `status --json` `/next_action/command` read `agentstack adopt`. `adopt` previews by default and declares a `--write` flag, so running the named form exits **0**, prints the manifest diff it WOULD apply, writes nothing — and the identical command is named again. An exit-0 infinite loop, which is the worse of the two shapes because nothing in the output says it failed. The correct string is `agentstack adopt --write`.
        - **[ ] G36** the consent gate cannot see a MACHINE manifest, and the ladder that points at it therefore never ends. State: the manifest at `~/.agentstack/agentstack.toml`, working directory `~` — the machine scope, which every other surface reads correctly. Each exit code read from the process directly: `status --json` reports `manifest.loaded = true` and names `agentstack lock --write` · that pins, exit 0 · `status --json` then names `agentstack trust .` · which exits **1**, "no agentstack manifest at or above ~ — run `agentstack init` first" · re-poll and the loop repeats. `trust --preview` and bare `trust` refuse identically, so no spelling of the gate can see the project, and the `agentstack init` its refusal suggests is wrong twice over: a manifest exists, and `init` refuses without a terminal (G33). **Not yet established, and deliberately not asserted either way: whether `trust` genuinely cannot REACH the machine manifest, or reaches it and REPORTS differently from `status` and `doctor`.** Those are different bugs with different fixes; whoever takes this establishes which one it is first. Related, and already recorded above as fixed at the extensions gate: "the extensions gate refused a machine manifest that no `trust` command can reach" — the same manifest, so check whether that fix bears on this before assuming a fresh cause.
12. [ ] When the bar is met — run the study, THEN fix the three blockers it names, publish, launch
      **Reworded 2026-08-08, and the old line must not be restored.** It read "re-pin the study kit, run it, fix its three blockers, publish, launch", which parses as a list of preconditions someone could work through *before* running anything. **The three blockers are the study's OUTPUT, not a backlog.** §7 of `docs/design/activation-study.md` holds three EMPTY slots — "Three most common blockers (by participant count, not severity)" — filled in after all five sessions, and `STRATEGY.md` calls it the three-blocker rule. They are unknowable in advance, so "fix its three blockers" cannot be started at all until the study has run; `STRATEGY.md` already states the steps in that order and this line now agrees with it.
      - **[~] Re-pin the study kit — the audit half is done; the version half cannot be done from here.** A re-pin pass on 2026-08-08 re-checked the kit against the real binary and corrected **six drifts** in place, each marked at the line it corrects (`docs/design/activation-study.md` §0.2): `apply` has left the journey · the `x gateway connect` bridge step the kit never mentioned, which sits *inside* the measured window · §8's latency baseline no longer supported · §8.1's "fixed in v0.17.1" claim, which does not hold in the routed shape · §4's closing undo task, which cannot be completed as worded · §5's reason for excluding the panel, whose stated reason has expired. Five instrument-design judgements were recorded and **deliberately not applied** (§0.3) — changing what the study measures is the maintainer's call, not a repair. No threshold moved: the five thresholds, the §6 metrics sheet, the §7 results template and the §7 pass condition are untouched.
      - **[ ] BLOCKED, and this is why item 12 stays open — there is no build to pin the study to.** The newest tag is `v0.18.0-rc.2` (`4afe274`, 2026-08-01) and §3 pins the study to it, but `main` is **127 commits ahead** of that tag, all of it on the participant's path: the consent gate extended to all five delivered kinds, routed delivery, `init --connect`, `NO_COLOR`, the whole G22–G36 "the next step names a command that then refuses" family, and the docs-site rebuild. The study would measure a product that no longer exists. **Re-pinning cannot be done from here: no newer tag exists** (§0.1, B-A). Cut the RC the study is to run against, then update §3's install line and the version floors in §3 and Appendix B3. Until then the kit's own header reads **DO NOT RUN THIS KIT UNTIL §0 IS CLEARED**, and nothing below it can start.
        - **Half-answered, so do not read B-A as the only gate: B-B, the version check that distinguishes nothing.** §0.1 records that `agentstack --version` printed `0.18.0-rc.2` at the tag *and* on `main`, so §3's and Appendix B3's "confirm `--version` ≥ 0.18.0-rc.2" passed on both binaries. `6a4cdf0` (2026-08-08) fixed the binary — `--version` now appends the commit and a dirty marker, with the old shape an exact prefix. It is **not closed**: `crates/cli/Cargo.toml` still reads `version = "0.18.0-rc.2"`, the JSON surfaces still carry the bare semver, and §0.1 has not been updated. Whoever cuts the RC in B-A settles both.
      - **The docs site rides with this release, not before it** (decided 2026-08-02; publishing earlier would describe a product nobody can run). **Corrected 2026-08-08:** this note used to say `.github/workflows/docs.yml` "deploys on any push to `main` touching `docs/**`". It does not, and has not since 2026-08-05 (`4d8c866`) — `deploy` is gated by `if: github.event_name == 'release' || github.event_name == 'workflow_dispatch'`, so a push to `main` reaches the `verify` checks only. A `pull_request` trigger was added 2026-08-08 and is excluded from `deploy` the same way; it exists because `verify` could previously only fail *after* a merge.
13. [ ] RMCP migration — recorded 2026-08-12, after the fact, because the work landed without an entry here.
      - **Dependencies, approved by the maintainer 2026-08-12** (CLAUDE.md: new dependencies need maintainer approval). `crates/mcp` takes `rmcp` pinned at `=3.1.2` and `process-wrap` `9.1` (`tokio1`), which bring **57 new packages into `Cargo.lock`** transitively. The approval is for that set at those versions; a version bump that widens the transitive set is a fresh decision, not a refresh. `rmcp` is the official MCP SDK and replaces the hand-rolled stdio protocol code; `process-wrap` owns child-process groups so a killed server takes its descendants with it. Boa stays isolated in `workflow` — untouched by this.
      - **[x] `PLAN.md` is design reference, not a second queue.** It was created during this migration and read as a competing board. It now carries a one-line header saying so; this file stays the only work queue.
      - **[x] The `doctor --json` activation drift it introduced.** `activation` was quietly re-valued to `live` / `not_live` under the unchanged `doctor-mode-v1` name and `schema_version 1`, so a consumer of the documented `locked` / `never_activated` read a locked project as never activated. `activation` is back to its shipped words and lockfile-derived; the runtime reading ships beside it as `live_state` under the new `doctor-liveness-v1` name (additive, no schema bump). `readiness`'s `never_activated` verdict, whose producing branch went with the same edit, is restored.
      - **[ ] Follow-up the migration still owes: a `cargo deny` pass** over the 57 added packages (licences and advisories).
      - **[x] `crates/mcp` is on the crate map.** The map is the file that says what each crate is for, and a crate absent from it is a crate nobody can review against a stated boundary. `docs/ARCHITECTURE.md` "Crate dependency rules" now carries the `mcp` paragraph beside `executor`'s: no internal edges by design, RMCP's types stop at the `Backend` trait, and it is a compatibility boundary rather than an enforcement one. The `crates/` tree in `CLAUDE.md` gained its line in the same edit. (Done 2026-08-12, alongside the same session's `runtime → core` edge removal, which the edge list also now reflects.)
