# acme-billing architecture

A small billing service. Agents are welcome to read and edit anything under
`src/` and `docs/`. Everything under `secrets/`, `personal-notes/`, and
`infra/prod/` is off-limits — see `.agentstack/agentstack.toml`.
