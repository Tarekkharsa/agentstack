SHARED-RULE: use Conventional Commits for every commit message, and run the
project's formatter before proposing a diff. This guidance is for every agent
CLI on the team, so it belongs in each harness's instructions file.
