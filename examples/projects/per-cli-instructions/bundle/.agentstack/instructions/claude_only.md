CLAUDE-ONLY-MARKER-7A31: when searching this repo, prefer the Grep tool over
shelling out to `grep`, and read files with the Read tool rather than `cat`.
This is Claude-Code-specific tooling advice; Codex should never see it.
