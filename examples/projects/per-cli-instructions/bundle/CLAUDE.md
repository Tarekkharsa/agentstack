# Acme Web — house rules

HANDWRITTEN-PROSE-KEEP-ME: this project uses pnpm, not npm. The database is
Postgres 16. Do not touch the `legacy/` directory without asking a human first.

These lines were written by a human and committed before AgentStack ever ran.
`agentstack instructions` must preserve them byte-for-byte and only ever edit
its own managed region, which it appends below.
